/**
 * Contains the classes with the main methods. There are two classes which are containing a main method. The first one allows to launch the RMI program for the tree implementation and ...
 */
package car.rmi.main;