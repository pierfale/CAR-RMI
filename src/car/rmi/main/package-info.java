/**
 * Contains the classes with the main methods. There are two classes which are containing a main method.
 *  The first one allows to launch the RMI program for the tree node implementation and the second for the graph node 
 */
package car.rmi.main;